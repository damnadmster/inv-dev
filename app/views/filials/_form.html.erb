<%= form_with(model: filial, local: true) do |form| %>
  <% if filial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filial.errors.count, "error") %> prohibited this filial from being saved:</h2>

      <ul>
      <% filial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :num, "Номер филиала (как в почте NNN)" %>
    <%= form.number_field :num %>
  </div>

  <div class="field">
    <%= form.label :name, "Наименование филиала" %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :code, "Код филиала (как в почте mfcNNN )" %>
    <%= form.text_field :code %>
  </div>

  <div class="actions">
    <%= form.submit  %>
  </div>
<% end %>
